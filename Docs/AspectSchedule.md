**📅 Aspect Schedule**

**Design & Architecture Document**

**Draft V0.1**

**0\. Цел на документа**

Този документ описва **новия календар (график)** в Aspect платформата:

- визуален модел
- взаимодействие с мишка (hover, drag, resize, snap)
- вътрешен модел на данните
- връзката между UI и логика
- решения, взети спрямо FMX и ограниченията му
- открити въпроси за следващи етапи (работно време, групиране, правила)

Документът служи за:

- референтна архитектура
- основа за по-нататъшна разработка
- контекст при бъдещи разговори / refactoring

**1\. Общ замисъл**

Aspect Schedule **не е просто календар**, а **интерактивен времеви редактор**, който:

- работи с **времеви интервали**
- позволява **директна манипулация** (drag / resize)
- визуализира **припокриващи се събития**
- е независим от конкретен тип преглед
- може да се използва в различни роли (регистратура, лекар, администратор)

Календарът **не записва директно в базата**, а:

- работи с **обекти в паметта**
- UI е отражение на тези обекти
- синхронизацията с БД е отделен етап

**2\. Технологичен избор**

**2.1 FMX вътре във VCL**

- Главното приложение е **VCL**
- Календарът е **FMX форма**, зареждана в контейнер
- Причини:
  - скалиране
  - висок FPS при drag
  - независимост от DPI
  - Canvas + Layout модел

FMX формата:

- няма собствен живот
- не държи глобално състояние
- може да се унищожава и пресъздава

**3\. Координатна система (ключово)**

**3.1 Логическо пространство**

Календарът работи в **логически координати**, които после се скалират:

- **X** → колона (тип преглед)
- **Y** → време

Примерни константи (като properties):

- COL_COUNT - брой колони (начално 7)
- COL_WIDTH - логическа ширина на колона (напр. 230)
- HOUR_HEIGHT - логическа височина на 1 час (напр. 80)
- GUTTER_WIDTH - ляв gutter за часовете
- HEADER_HEIGHT, FOOTER_HEIGHT

Логическа височина:

DAY_HOURS \* HOUR_HEIGHT + HEADER + FOOTER

**3.2 Скалиране**

- Всичко е дете на TScaledLayout
- Скалирането е **центрирано в позицията на мишката**
- ScaleDyn се поддържа ръчно
- ScrollBox работи върху скалираното съдържание

👉 Това позволява:

- CAD/CAM стил зум
- липса на „скачане"
- стабилни координати

**4\. Визуална структура**

**4.1 Основни слоеве**

- **PaintBox (фон)**
  - хоризонтални линии (часове / half-hour / quarter)
  - вертикални линии (колони)
  - gutter с часовете (рисуван, не layout)
- **Layout слой (събития)**
  - всеки event е отделен TLayout
  - абсолютна позиция в логическото пространство

**5\. Модел на събитие**

**5.1 Клас TAspectScheduleEvent**

TAspectScheduleEvent = class

Root: TLayout; // визуален контейнер

Body: TRectangle; // основен правоъгълник

Caption: TText; // заглавие

ResizeTopLyt: TLayout;

ResizeBottomLyt: TLayout;

TypeEvent: Integer; // тип / колона

StartTime: TTime;

EndTime: TTime;

DataPos: Cardinal; // връзка с БД (по-късно)

end;

**5.2 Жизнен цикъл**

- Контролите **се клонират**, не се създават всеки път
- Никога не се унищожават
- При „скриване":
- Parent := nil
- При нови данни:
  - преизползване
  - нови стойности
  - repaint

👉 Това гарантира:

- 0 flicker
- стабилен performance
- лесен sync

**6\. Добавяне на събитие**

**6.1 Входни параметри**

AddEvent(

Index,

TypeEvent,

StartTime,

EndTime

)

**6.2 Поведение**

- ако няма достатъчно обекти → clone
- ако има → reuse
- позиционирането става **само** от Start/EndTime
- UI не пази „истината"

**7\. Hover логика**

**7.1 FHover структура**

- Col
- Quarter
- Valid

Изчислява се от:

- позицията на мишката
- логическите размери

Hover:

- **не създава контрол**
- служи за preview / създаване

**8\. Създаване с клик**

- MouseUp върху PaintBox
- ако FHover.Valid
- създава се event с минимален интервал

**9\. Resize (най-сложната част)**

**9.1 Защо SelectionPoint**

FMX:

- не позволява глобално mouse capture
- child контроли „изяждат" събитията

Решение:

- **един SelectionPoint**
- мести се под мишката
- става child на активния layout
- стартира native drag/track

**9.2 Режими**

rmNone

rmTop

rmBottom

Определят се по layout-а (StyleName).

**9.3 Track логика**

- Track дава **позиция на центъра**
- Y се преобразува → време чрез YToTime
- Променя се само:
  - StartTime или EndTime
- UI се repaint-ва

**9.4 Ограничения**

- MIN_SLOT (по подразбиране 15 мин)
- не може:
  - под минимума
  - извън работната област
- курсорът може да се движи, но правоъгълникът спира  
    (както в Google Calendar)

**10\. Drag на цялото събитие**

(следващ етап)

- drag върху Body
- променя Start/EndTime със същата разлика
- сменя колона при хоризонтално движение
- snap към колони

**11\. Припокриващи се събития (Overlaps)**

**11.1 Два режима (наблюдавани в Google)**

- **Прост**
  - равномерно делене на ширината
  - всички са видими
- **Интелигентен (title-aware)**
  - заглавието има приоритет
  - ширината се адаптира
  - текстът „заобикаля" съседния блок

**11.2 Планирано решение в Aspect**

- изчисляване на overlap групи
- за всяка група:
  - динамична ширина
  - отстъп според реалната височина на текста
- използване на **собствения алгоритъм за сричкопренасяне**

⚠️ **Важно**  
Това **не се прави в OnPaint**, а:

- при промяна на данни
- резултатът се кешира

**12\. Текст и четимост**

- Caption има:
  - оригинален текст
  - preprocessed (wrap / syllable break)
- височината на текста се използва при layout
- цел: **минимален отстъп + максимална четимост**

**13\. Работно време (следващ голям етап)**

Наблюдения:

- сегашният (стар) модел е сложен и фрагментиран
- няколко екрана → трудно за потребителя

Планирано:

- работното време да се визуализира **в същия календар**
- забранени зони (disabled slots)
- събития не могат да се създават извън тях

⚠️ Отворен дизайн въпрос:

- дали работното време е отделен layer
- или специален тип event

**14\. Навигация и роли**

- календарът живее в **работния екран**
- навигационно дърво:
  - по лекар
  - по тип
  - по период
- изборът в дървото определя:
  - какво се зарежда
  - какво е активно

**15\. Статус на реализацията (към момента)**

✅ Скалиране с център в мишката  
✅ Scroll + zoom без трепкане  
✅ Hover grid  
✅ Създаване на събитие  
✅ Resize top/bottom  
✅ MIN_SLOT  
✅ Boundary protection

⏳ Drag whole event  
⏳ Overlap layout  
⏳ Work hours  
⏳ Persistence / import

**16\. Заключение**

Aspect Schedule вече:

- **работи като истински редактор**
- не е просто визуализация
- е стабилна основа за разширяване

По-нататъшната работа е **еволюция**, не пренаписване.

**📌 Забележка**

Този документ е **жив**.  
Следващи версии: V0.2, V0.3… с добавени решения.
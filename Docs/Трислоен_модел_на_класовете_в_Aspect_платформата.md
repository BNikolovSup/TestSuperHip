**🔷 Трислоен модел на класовете в Aspect платформата**

Aspect платформата използва **трислоен модел на класовете**, който е фундаментален за начина, по който системата работи, мащабира се и остава бърза.

Трите слоя са:

Base Classes

↓

Generator Classes

↓

Real (Business) Classes

Всеки слой има **ясна отговорност** и **строги граници**.

**🔷 1. Base класове (ядро на формата)**

Base класовете дефинират **бинарния формат и механиката на съхранение**:

- ADB (данни)
- LNK (връзки)
- CMD (команди)

Тук се намират методи като:

- SaveData
- SaveNull
- SaveHeaderData
- ReadData
- low-level offset логика

**Важно:**

- Base класовете **не съдържат бизнес логика**
- Те дефинират _как изглежда записът във файла_
- Това е **canonical слойът** - истината за формата е тук

**🔷 2. Generator класове (унифицирано поведение)**

Generator класовете се **генерират автоматично** от Aspect Table Generator.

Те:

- наследяват Base класовете
- добавят **унифицирано поведение** за всички аспекти
- осигуряват еднакъв API за:
  - insert
  - update
  - read
  - search
  - indexing
  - UI binding
  - CMD replay

Generator класовете **не са писани за четимост**, а за:

- повторяемост
- еднакво поведение
- механично разширяване

**Пример:**

TDoctorItem, TPatientItem, TExamItem и т.н.  
Всички те имат:

- еднаква структура
- еднакви методи
- еднаква логика за запис и четене

**🔷 3. Real (Business) класове**

Real класовете наследяват Generator класовете и добавят **реална бизнес логика**.

Пример:

- пациентът съдържа прегледи
- прегледите съдържат документи
- документите съдържат терапии, направления и др.

**Характеристики:**

- тежки обекти
- създават се **само при нужда**
- използват се когато лекарят реално работи с тях

**Критично важно:**

При първоначално зареждане:

- ADB и LNK файловете се отварят
- **колекциите са празни**
- зареждат се само:
  - малки колекции
  - индекси
  - навигационни структури

Това прави:

- стартирането изключително бързо
- динамичните екрани възможни
- работа с големи бази без забавяне

**🔷 Генераторните класове НЕ са ORM**

Aspect платформата **съзнателно не използва SQL ORM модел**.

Причините:

- динамични екрани, които се генерират от аспекти
- навигация по дървета (пациент → преглед → документ)
- изключително бързо зареждане (под 1 ms за екран)
- офлайн работа
- пълна история чрез CMD

SQL модел с ORM:

- би зареждал твърде много данни
- би изисквал сложни JOIN-ове
- не би позволил тази динамика и скорост

Generator класовете **са самата „база данни"**, но в обектна форма.

**🔷 Еволюция на генераторните класове**

Генераторните класове **се развиват**, а не се пишат окончателно.

Реалният процес е:

- Нова функционалност се разработва в конкретен клас  
    (пример: TDoctorItem)
- Тестване, оптимизация, стабилизиране
- След като логиката стане ясна:
  - тя се пренася в шаблона
- Генераторът обновява **всички класове автоматично**

Base и Real слоевете **остават непроменени**.

**🔷 Legacy и остатъчни елементи**

В Generator класовете могат да присъстват:

- параметри, които вече не се използват
- методи с историческа причина
- код, останал от по-стари версии

Това е **очаквано и допустимо**, защото:

- форматът е стабилен
- backward compatibility е важна
- кодът е еволюирал с години

Премахването на такива елементи **не трябва да става автоматично**.

**🔷 Защо този модел изглежда „странен", но работи**

В началото:

- кодът изглежда тежък
- генераторните класове са големи
- няма SQL

С времето:

- нови функционалности се добавят **без да се пипа базата**
- динамичните екрани се генерират автоматично
- скоростта остава константна
- сложността се изплаща

Това е **инвестиционен архитектурен модел**, не бързо CRUD решение.

**🔷 Обобщение**

- Base класовете дефинират формата
- Generator класовете дефинират поведението
- Real класовете дефинират бизнеса
- Генераторът е инструмент за мащабиране
- Кодът расте контролирано
- Данните са обекти, не редове
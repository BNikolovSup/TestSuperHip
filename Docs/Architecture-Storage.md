📘 Hippocrates 360 – Storage Architecture Overview

Този документ описва вътрешната архитектура на файловата база от данни в системата Хипократ 360.
Тя се състои от няколко бинарни файла, оптимизирани за:

висока производителност

минимална загуба на данни

memory-mapped достъп

лесно възстановяване след срив

работа в реално време

поддържане на сложни структури (медицински данни)

Архитектурата е напълно custom и НЕ е базирана на SQL.

#️⃣ 1. Основни файлове на базата

Системата използва четири бинарни файла:

Файл	Описание
.adb	Основният файл с данни (Header + Meta + Data)
.lnk	Индексен файл (виртуално дърво, memory mapped)
.cmd	Команден журнал – записва всяка промяна
.temp	Временен команден файл за незаписани промени
#️⃣ 2. ADB файл – Основни данни

ADB файлът съдържа три части:

2.1 Header (100 bytes)

Системна информация:

начало и размер на Meta секцията

начало и размер на Data секцията

версия

технически параметри

2.2 Meta секция

В Meta секцията се съдържа информация за всеки запис:

[Version:2 bytes]
[CollectionType:2 bytes]
[FieldPointer1:4 bytes]
[FieldPointer2:4 bytes]
...


Meta е таблица от pointer-и към Data частта

Всеки pointer сочи към начало на блок данни за конкретно поле

Meta поддържа версияция

2.3 Data секция

Съдържа реалните данни:

[HistoryPointer:4 bytes]
[Length:2 bytes]
[Value bytes…]
[Padding (за подравняване)]


Всеки запис има:

history pointer (старото място на данните)

дължина

стойност

подравняване

Това позволява:

journaling

undo/redo

възстановяване на файловете

#️⃣ 3. LNK файл – Индекс (VirtualStringTree)

LNK файлът е бинарна версия на TVirtualStringTree.

всеки възел = 64 bytes

съдържа:

индекс в колекцията

тип (vid)

DataPos (къде се намира Meta за този запис)

целият файл е memory-mapped

зарежда се изключително бързо (~4 млн възела за ~200 ms)

LNK представлява индекс и връзки между обектите.

#️⃣ 4. CMD файл – Команден журнал (transaction log)

CMD файлът е ключов компонент и изпълнява няколко роли:

✔️ 4.1 Нулева загуба на данни (Zero-Loss)

Всяко Insert / Update записва команда:

дължина

стойност

тип операция

тип колекция

версия

променени полета

При срив:
ADB и LNK се възстановяват от CMD.

✔️ 4.2 Репликация – прехвърляне на данни между потребители

CMD е последователност от операции, които могат да се изпратят към друг потребител и да се изпълнят върху неговата база.

Така два потребителя синхронизират данните си без централен сървър.

✔️ 4.3 Транзакционен лог

CMD играе ролята на „журнал“:

записва операцията преди тя да бъде приложена

позволява възстановяване при грешка

позволява криптиране на чувствителни операции

#️⃣ 5. TEMP файл – временни промени

TEMP файлът е временна версия на CMD:

записва незапазени промени

след commit → влиза в CMD

при crash → възстановява последното състояние

#️⃣ 6. Insert / Update механизъм (SaveData)
SaveData извършва:

Изчисляване на новото място в Data секцията

Запазване на предишната стойност (history pointer)

Обновяване на pointer-а в Meta секцията

Записване на дължина и стойност

Подравняване (alignment)

Записване на командата в CMD файла

Това е механизмът за:

journaling

транзакции

репликации

възстановяване

#️⃣ 7. Тройно наследяване (3-layer architecture)

Системата използва трислойна архитектура:

1. Base Layer
TBaseItem
TBaseCollection


Обща логика: ADB, Meta, Data, CMD, SaveData, Update, Insert

2. Generated Layer

Автоматично генерирани класове от DDL:

съдържат структурата на таблиците

съдържат property индексите

съдържат PRecord

3. Real Layer

Ръчни бизнес класове:

наследяват генерираните

съдържат връзките между обектите

съдържат бизнес логика

Файл: RealObj.RealHipp.pas

📄 ГОТОВ ФАЙЛ: Docs/Architecture-Network.md

Копирай това в:

Docs/Architecture-Network.md

📘 Hippocrates 360 – Network Architecture

Системата използва custom peer-to-peer модел.
Не използва клиент/сървър.

Моделът се нарича:

🔶 Изпълнител / Разпределител
#️⃣ 1. Изпълнител (Executor)

Знае как да:

изпълнява бизнес логика

прави Insert и Update

поддържа ADB / LNK / CMD

валидира медицински данни

криптира чувствителна информация

Изпълнителят е „умният възел“.

#️⃣ 2. Разпределител (Distributor)

Знае само да:

приема CMD команди по сокети

разбира за кого са предназначени

препраща ги

доставя ги точно

Няма бизнес логика.

Разпределителят е „сервитьорът“:

„Почерпи оня човек“ — той просто носи почерпката.

#️⃣ 3. Комуникация по сокети

След установяване на връзката:

няма клиент

няма сървър

всеки може да изпраща команди

всеки може да ги получава

комуникацията е симетрична

#️⃣ 4. Ясни аналогии
❌ Модел Клиент/Сървър:

Сървърът чака

Клиентът пита

Бавно

Ненадеждно за real-time

✔️ Модел Изпълнител/Разпределител:

Разпределителят доставя командата веднага

Изпълнителите си говорят директно

Няма polling

Real-time обработка

Идеално за чувствителни данни

#️⃣ 5. Криптирани команди

Някои команди са явни

Някои са криптирани (медицинска чувствителна информация)

Разпределителят НЕ знае съдържанието

Само изпълнителите могат да ги разшифроват

**📘 META DOCUMENT V1.1 - Aspect Platform (Unified Edition)**

**Статус: Работна версия (Ready for GitHub)**

**Обединява V1 + V2 + V3 + новите разширения (UI, динамични екрани, fuzzy search, history, терапия, NZIS и др.)**

**🟥 0. Цел на документа**

Това е _централният технически документ_ за **Aspect Platform** - архитектурата, файловия модел, импорта, историята, UI, динамичните екрани и Search Engine.

Използва се за:

✔ зареждане на нови ChatGPT сесии  
✔ документация за GitHub  
✔ програмен reference за генератора, ADB архитектурата и NZIS поддръжката

**🟦 1. Основна философия**

**1.1. „От всички гледни точки" → Аспекти**

Aspect платформата е създадена за локална работа с:

- супер-бърз достъп до данни (memory mapped)
- без SQL
- без нужда от централен сървър
- авто-история на всички стойности (неограничена)
- динамични UI екрани
- ролеви права върху данните („виждам само за което отговарям")

**1.2. Мрежов модел: Изпълнител ↔ Разпределител**

- Изпълнител = локален клиент → работи в real time
- Разпределител = централен хъб → събира командни файлове и разпраща

Командният файл е универсален носител на промени.

**1.3. Предимства / Ограничения**

⚡ Предимства:

- мигновено търсене
- търсене по дърво (object graph)
- без SQL индексни разходи
- без ORM
- автоматична история
- перфектна за офлайн среда

🔸 Ограничения:

- размер на memory mapped файлове
- сложно управление на структурата

**🟩 2. ADB файл - архитектура**

ADB = Aspect DataBase

**2.1. Сегменти**

| **Сегмент** | **Описание** |
| --- | --- |
| Header | 100B с позиции на блокове |
| Metadata | offsets за полетата |
| Data Segment | реалните стойности |
| History Chain | 4B pointer към предишна стойност |
| Reserved | бъдещи разширения |

**2.2. Важни формули**

valuePtr = buf + DataPos + 4 \* FieldIndex

historyPtr = valuePtr - 4

**2.3. История**

Всеки write добавя нова стойност и премества pointer-а.

веригата на историята:

200 → 320 → 512 → 0

**🟫 3. Колекции и елементи**

**3.1. TBaseCollection**

Съдържа:

- Buf
- FieldCount
- StreamComm (записване на команди)
- ListNodes / ListDataPos
- OnGetCell / OnSetCell

**3.2. TBaseItem**

Съдържа:

- DataPos
- PRecord (временна структура при редакция)
- SetProp (полета с промени)

**🟧 4. DataPos, PosData**

DataPos съдържа:

| **offset** | **съдържа** |
| --- | --- |
| \-4 | тип (ctXXX / ctOld / ctDel) |
| 0   | history pointer |
| +4\*N | стойност на поле N |

History позволява да рендерираме:

- старо състояние
- сравнение в диф
- какво е било при даден документ

**🟪 5. Колекционни типове**

Трите основни състояния:

| **Тип** | **Значение** |
| --- | --- |
| ctXXX | активен |
| ctXXXOld | непотвърден след импорт |
| ctXXXDel | окончателно изтрит |

**🟥 6. Командният файл**

**6.1. Съдържание**

Съдържа атомарни операции:

- Insert
- Update
- Delete
- LogicalSets
- Позиции + стойности

**6.2. Ролята му**

- възстановяване на ADB/LNK от 0
- изпращане промени към централен хъб
- сравнение / диф
- логика за синхронизация м/у машини

**6.3. Връзка с AddNewNode**

При всеки нов възел:

- LNK запис
- ADB запис
- CMD запис

**🟦 7. Генераторът (DDL → Pascal)**

**7.1. Вход: DDL**

Пример:

EGN=AnsiString

ID=Integer

Logical=tLogicalDoctorSet:IS_ZAMESTNIK,IS_NAET

**7.2. Изход:**

- Table.Doctor.pas
- InsertDoctor
- SaveDoctor
- ReadCmdDoctor
- FillPropDoctor
- BuildKeyDict
- ImportXMLNzis / UpdateXMLNzis (ако е CL)

**7.3. Специално за NZIS (CLxxx)**

Добавя:

- ctXXXOld / ctXXXDel
- диф логика
- XML mapping
- автоматично сдвояване по ключове

**🟧 8. NZIS архитектура**

**8.1. Принцип**

В XML всички кодови полета са CodeableConcept → с мапинг към:

- CL009 (лекарства)
- CL022 (анализни видове)
- CL011 (МКБ)
- CL024 (диагнози)
- CL037 (списъци)
- … и др.

**8.2. Импорт**

Стратегия:

- Всички записи → ctOld
- XML се минава един по един
- Ако има ключ → update
- Ако няма ключ → insert
- ctOld без update → ctDel

**8.3. Update**

В UpdateXMLNzis:

- сравняваме полетата
- setProp пълни само различните
- не се прави insert в Data блока (освен при нови)

**🟩 9. Diff система**

Въвежда TDiffKind:

dkNone

dkChanged

dkNew

dkForDeleted

dkDeleted

**9.1. Визуализация в TTeeGrid:**

| **diff** | **цвят** |
| --- | --- |
| dkNew | зелено |
| dkChanged | оранжево |
| dkDeleted | червено, strikeout |
| dkForDeleted | сиво |
| dkNone | нормално |

Tooltip → показва старите стойности (от history chain).

**🟥 10. UI: TTeeGrid, динамични екрани и Filter Tree**

**10.1. TTeeGrid**

Всеки Grid се попълва чрез:

OnGetValue

OnSetValue

TVirtualModeData

**10.2. Динамични екрани**

Екраните се генерират според:

- аспектското дърво
- комбинацията AspectNode + Collection + Field
- текущия възел от АДБ дървото
- контекст (преглед → рецепта → терапия → диагноза)

Всяка форма се създава автоматично и знае:

- към кой item е вързана
- как да записва стойности
- как да чете от DataPos или от PRecord

**10.3. Filter Tree UI**

Съдържа:

- Object nodes
- Field nodes
- Operator nodes
- ObjectGroup nodes
- FilterItem nodes

Поддържа:

- 3-state checkboxes
- динамични групи
- автоматично генериране според АДБ структурата

**10.4. Magic Index (PathIndex)**

Формула:

Sig = hash(path of virtual nodes)

Държи:

- за всеки тип обект → всички възли в ADB
- за филтърното дърво → само активните възли

Join се прави чрез:

AspectsLinkPatPregFile.PathIndex.JoinWith(AspectsFilterLinkFile)

**🟦 11. Search Engine (V4)**

**11.1. Стратегии**

- AND → всеки следващ филтър прави множестово подмножеството на предишното
- OR → събира UNION от bucket-и
- SMART → ако потребител пише „Иван" → всеки нов символ намаля резултата
- PRIORITY → полетата могат да са бързи (EGN) или бавни (анамнеза)

**11.2. Типове проверки**

- equality (super fast, CRC32)
- startswith (Boyer-Moore + direct pointer)
- contains (fast BMH)
- fuzzy (weighted Levenshtein)

**11.3. Multi-core подход**

Search може да се разделя на блокове:

- 4 CPU threads → еднакви блокове
- всеки thread работи само с неговия bucket

**11.4. Нишка за търсене**

TSearchThread:

- interruptible
- stop/start при промяна
- попълва ListDataPos за всяка колекция
- изпраща „готово" към UI

**🟥 12. Fuzzy Search Engine**

Въвежда се модул **uFuzzyMatch.pas**

Поддържа:

- Damerau-Levenshtein
- Prefix score
- Token distance
- Word swap (Lname Fname)

**🟦 13. History Chain - техники**

Историята се използва за:

- диф
- визуализация
- проверка къде е участвал записът
- старо състояние към конкретен документ

Четене на стара стойност:

ptr := DataPos + 4\*index;

prevPtr := (ptr - 4)^;

**🟪 14. NZIS Терапия и Рецепти (Medication)**

Моделът, описан в чат-а, е включен тук.

**14.1. Разделяне на таблиците**

FREE_RECIPE → основа (header)  
FREE_RECIPE_ROWS → терапия / медикаменти

**14.2. Аспектски модел**

Recipe

├── TherapyGroups

│ └── TherapyItems

└── ValidationHistory

**14.3. Логикали**

Логическите флагове се вземат от:

- financing source
- printed
- pregnant
- status
- validation
- protocol

**14.4. Номенклатури**

Номенклатурите в рецептата сочат към:

- CL009 (лекарства)
- CL010 (форми)
- CL020 (period_unit)
- CL013 (route)
- CL024 / CL011 (МКБ)

**🟧 15. Динамични UI форми за Терапия**

Автоматично генерирани компоненти:

- списък с медикаменти
- дозировка
- форма на прием
- период
- инструкции
- генериране на текст за интерпретация

**🟦 16. Правила за бъдещи ChatGPT сесии**

**16.1. Как се стартира нов чат**

Започва се с фразата:

Зареждам META DOCUMENT V1.1 за проект Аспекти:

&lt;тук поставяш документа&gt;

**16.2. ChatGPT трябва да:**

- работи в рамките на документа
- допълва документа само по заявка
- предлага генераторен код с !Placeholders!
- следва аспектския модел (ADB/LNK/CMD)
- разбира Object Tree логиката
